/**
 * Local Declarations for Typescript
 *
 * If a package that you want to use doesn’t bundle declarations nor is it
 * available on DefinitelyTyped, create a local declaration here.
 * An example declaration:
 *
 *   declare module 'querystring' {
 *     export function stringify(val: object): string
 *     export function parse(val: string): object
 *   }
 */
